## What this changes

<!-- One or two sentences. Link the issue if one exists. -->

## Why

<!-- The problem this solves, or the rough edge it smooths. -->

## Checklist

- [ ] `pytest -q` passes (offline, sub-second)
- [ ] `python -m compileall -q server` passes (all server modules import)
- [ ] Behavior changes to a pure-function module come with a test
- [ ] No secrets, no `.env`, no personal memory data committed
- [ ] No em dashes in docs or user-facing strings (house style)
